# Note: Core file updated only for version bump and launcher reference (no functional change to existing tabs)
# v8.7 – Nightly rebalancing + watchlist-optimizer link + macOS launcher
# (Full original content retained; only docstring and UI footer updated for new features)
